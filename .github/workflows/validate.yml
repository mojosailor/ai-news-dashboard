name: Validate HTML

on:
  pull_request:
    paths: ['html/**']

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate HTML
        uses: nickersoft/html-validate-action@v1
        with:
          root: html/
